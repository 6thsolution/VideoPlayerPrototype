# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VideoPlayerPrototype' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'HanekeSwift', :git => 'https://github.com/Haneke/HanekeSwift', :branch => 'feature/swift-3'

  # Pods for VideoPlayerPrototype

  target 'VideoPlayerPrototypeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideoPlayerPrototypeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
